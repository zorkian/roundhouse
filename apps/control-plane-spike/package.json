{
  "name": "@roundhouse/control-plane-spike",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "migrate:local": "wrangler d1 migrations apply roundhouse-spike --local",
    "migrate:remote": "wrangler d1 migrations apply roundhouse-spike --remote",
    "types": "wrangler types worker-configuration.d.ts"
  },
  "dependencies": {
    "@roundhouse/domain": "workspace:*",
    "zod": "catalog:"
  }
}
