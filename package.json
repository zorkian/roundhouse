{
  "name": "roundhouse",
  "version": "0.0.0",
  "license": "Apache-2.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "tsc -b",
    "check": "prettier --check . && node scripts/check-license-headers.mjs && tsc -b --pretty false && node --check containers/agent-runner/runner.mjs && vitest run",
    "deploy:development": "pnpm build && wrangler deploy --config apps/model-broker/wrangler.jsonc && wrangler d1 migrations apply roundhouse-v2-development --remote --config apps/control-plane/wrangler.jsonc && wrangler deploy --config apps/control-plane/wrangler.jsonc",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "license:check": "node scripts/check-license-headers.mjs",
    "runner:check": "node --check containers/agent-runner/runner.mjs",
    "test": "vitest run",
    "typecheck": "tsc -b --pretty false"
  },
  "devDependencies": {
    "@achingbrain/nss": "^0.0.1",
    "@cloudflare/workers-types": "5.20260714.1",
    "@types/node": "^24.0.14",
    "chrome-aws-lambda": "10.1.0",
    "prettier": "^3.6.2",
    "puppeteer-core": "10.1.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "wrangler": "4.112.0"
  }
}
